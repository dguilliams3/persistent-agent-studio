/**
 * Core Types — Barrel Export
 *
 * One type per file. This barrel re-exports everything for clean
 * consumer imports from '@persistence/core'.
 *
 * @antipattern Do NOT add type definitions to this file — create a new .ts file.
 */
export type { PersonaConfig } from './PersonaConfig';
export type { MeterConfig } from './MeterConfig';
