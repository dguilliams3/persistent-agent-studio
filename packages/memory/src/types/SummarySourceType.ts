/**
 * What the summary was made from.
 *
 * 'history' = First-pass summarization (history entries to summary)
 * 'summary' = Meta-summarization (summaries to meta-summary)
 */
export type SummarySourceType = 'history' | 'summary';
